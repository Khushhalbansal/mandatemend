# Secrets — never commit (CLAUDE.md §6)
.env
*.pem
*.key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Data — generated training set is reproducible from a seed, not committed.
# The FROZEN held-out batch IS committed (it is the contract; see CLAUDE.md §1.3).
data/training_set.parquet
data/training_set.json
data/*.tmp

# Local DB volume
pgdata/

# OS
.DS_Store
Thumbs.db

# Node (console assets are vendored, no build step)
node_modules/

# local live round-trip artifacts (evidence lives in CHANGELOG.md)
logs/live_audit.sqlite
logs/last_live_roundtrip.json
logs/_*
logs/run_batch.sqlite
